[project]
name = "server-base-cli"
version = "0.1.0"
description = "server_base の運用CLI/TUI"
requires-python = ">=3.10"
dependencies = [
    "textual>=0.60",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.uv]
package = false

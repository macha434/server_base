# SSL Directory

Place your SSL certificates and keys in this directory.

Example structure:
- cert.pem - SSL certificate
- key.pem - Private key

Note: Actual certificate files are excluded from git tracking for security.

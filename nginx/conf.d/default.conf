# Default/root configuration
# ルート（/）へのアクセスはここで定義
server {
    listen 80;
    server_name _;

    # ヘルスチェック用エンドポイント
    location /health {
        access_log off;
        return 200 "OK";
        add_header Content-Type text/plain;
    }
}
